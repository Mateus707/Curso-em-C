#include <stdio.h>
#include <stdlib.h>
#define pi 3.14 //declaração de outro tipo de constante
 int main(){

    // const float pi = 3.14;//constates devem obrigatoriamente receber um valor assim que criadas 
    // int soma = pi+14;
    // printf("Constante %f \n",pi);
    // printf("Inteiro %d \n",soma);

    // printf("PI = %f \n",pi); // a diferença de define para const, o const voce declara uma variavel e o computador
                                //vai na memoria e guarda a sua constante, e ela fica na memoria e não pode ser sobreescrevida, já 
                                // o define funciona uma substituição qunado vc compila o progrmaa o computador proucura onde a palavra 
                                //no caso pi aparace ela é substituida pelo valor.
     

    system("Pause");
    return 0;
 }